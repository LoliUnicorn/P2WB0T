function play(url: text) :: boolean:
	set {_g} to getGuild()
	set {_b} to {bot::name} parsed as bot
	loop queue of {_b} in {_g}:
		if "%url of loop-value%" is "%{_url}%":
			return false
	play "%{_url}%" in {_g} with {bot::name}
	return true